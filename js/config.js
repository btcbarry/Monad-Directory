const CONFIG = {
    API_ENDPOINT: '/api/chat',  // Will handle API requests through our server
    MAX_CONTEXT_LENGTH: 10,     // Maximum number of messages to keep in context
    TYPING_DELAY: 500,         // Delay before showing typing indicator
    SUGGESTIONS_COUNT: 3       // Number of suggestions to show
}; 